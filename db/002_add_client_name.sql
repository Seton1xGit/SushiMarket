-- ============================================================================
-- Add client_name column to order_events.
-- Имя клиента нужно для персонализации сообщений в WF-B/C/D, в т.ч. в
-- order_closed-ветке (там данных из листинга уже нет, читаем из БД).
-- ============================================================================

ALTER TABLE order_events ADD COLUMN IF NOT EXISTS client_name TEXT;
